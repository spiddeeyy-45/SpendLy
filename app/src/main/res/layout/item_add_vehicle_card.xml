<?xml version="1.0" encoding="utf-8"?>
<!-- res/layout/item_vehicle_card_add.xml -->
<!-- "Add Vehicle" card — same width as item_vehicle_card.xml -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="200dp"
    android:layout_height="wrap_content"
    android:layout_marginEnd="14dp">

    <eightbitlab.com.blurview.BlurView
        android:id="@+id/blurAddCard"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <LinearLayout
            android:id="@+id/addVehicleCardRoot"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="center"
            android:padding="20dp"
            android:background="@drawable/bg_add_vehicle_card"
            android:clickable="true"
            android:focusable="true"
            android:foreground="?attr/selectableItemBackgroundBorderless">


            <!-- Dashed circle icon -->
            <FrameLayout
                android:layout_width="64dp"
                android:layout_height="64dp"
                android:background="@drawable/bg_add_circle_dashed"
                android:layout_marginBottom="12dp">

                <!-- Plus icon — two rectangles forming a + -->
                <View
                    android:layout_width="2dp"
                    android:layout_height="22dp"
                    android:layout_gravity="center"
                    android:background="#A99DFF"/>

                <View
                    android:layout_width="22dp"
                    android:layout_height="2dp"
                    android:layout_gravity="center"
                    android:background="#A99DFF"/>

            </FrameLayout>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Add Vehicle"
                android:textColor="#A99DFF"
                android:textSize="13sp"
                android:textStyle="bold"
                android:layout_marginBottom="2dp"/>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Tap to add new"
                android:textColor="#50B4B9DC"
                android:textSize="10sp"/>

        </LinearLayout>

    </eightbitlab.com.blurview.BlurView>

</FrameLayout>